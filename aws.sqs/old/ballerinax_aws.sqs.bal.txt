// ============================================================
// Library: ballerinax/aws.sqs
// Amazon Simple Queue Service (SQS) is a fully managed message queuing service that enables you to decouple and scale microservices, distributed systems, and serverless applications. It provides a reliable and highly scalable way to exchange messages between different components. The AWS SQS connector allows you to interact with SQS queues, enabling efficient message processing in the cloud.
// ============================================================
import ballerinax/aws.sqs;

// --- README ---
## Overview

Amazon Simple Queue Service (SQS) is a fully managed message queuing service that enables you to decouple and scale microservices, distributed systems, and serverless applications. It provides a reliable and highly scalable way to exchange messages between different components. The AWS SQS connector allows you to interact with SQS queues, enabling efficient message processing in the cloud.

### Key Features

- Fully managed and highly scalable message queuing
- Support for both standard and FIFO queues
- Reliable message delivery with dead-letter queue support
- Secure communication with AWS IAM authentication
- Simplified message production and consumption
- GraalVM compatible for native image builds

## Setup guide

### Login to AWS Console

Log into the [AWS Management Console](https://console.aws.amazon.com/console). If you don’t have an AWS account yet, you can create one by visiting the AWS [sign-up](https://aws.amazon.com/free/) page. Sign up is free, and you can explore many services under the Free Tier.

### Create a user

1. In the AWS Management Console, search for IAM in the services search bar.
2. Click on IAM

   ![create-user-1.png](https://raw.githubusercontent.com/ballerina-platform/module-ballerinax-aws.sqs/refs/heads/master/docs/setup/resources/create-user-1.png)

3. Click Users

   ![create-user-2.png](https://raw.githubusercontent.com/ballerina-platform/module-ballerinax-aws.sqs/refs/heads/master/docs/setup/resources/create-user-2.png)

4. Click Create User

   ![create-user-3.png](https://raw.githubusercontent.com/ballerina-platform/module-ballerinax-aws.sqs/refs/heads/master/docs/setup/resources/create-user-3.png)

5. Provide a suitable name for the user and continue

   ![specify-user-details.png](https://raw.githubusercontent.com/ballerina-platform/module-ballerinax-aws.sqs/refs/heads/master/docs/setup/resources/specify-user-details.png)

6. Add necessary permissions by adding the user to a user group, copy permissions or directly attach the policies. And click next.

   ![set-user-permissions.png](https://raw.githubusercontent.com/ballerina-platform/module-ballerinax-aws.sqs/refs/heads/master/docs/setup/resources/set-user-permissions.png)
7. Review and create the user

   ![review-create-user.png](https://raw.githubusercontent.com/ballerina-platform/module-ballerinax-aws.sqs/refs/heads/master/docs/setup/resources/review-create-user.png)

### Get user access keys

1. Click the user that created

   ![users.png](https://raw.githubusercontent.com/ballerina-platform/module-ballerinax-aws.sqs/refs/heads/master/docs/setup/resources/users.png)

2. Click `Create access key`

   ![create-access-key-1.png](https://raw.githubusercontent.com/ballerina-platform/module-ballerinax-aws.sqs/refs/heads/master/docs/setup/resources/create-access-key-1.png)

3. Click your use case and click next.

   ![select-usecase.png](https://raw.githubusercontent.com/ballerina-platform/module-ballerinax-aws.sqs/refs/heads/master/docs/setup/resources/select-usecase.png)

4. Record the Access Key and Secret access key. These credentials will be used to authenticate your Ballerina application with Amazon SQS.

   ![retrieve-access-key.png](https://raw.githubusercontent.com/ballerina-platform/module-ballerinax-aws.sqs/refs/heads/master/docs/setup/resources/retrieve-access-key.png)

## Quickstart

To use the `aws.sqs` connector in your Ballerina project, modify the .bal file as follows.

### Step 1: Import the module

```ballerina
import ballerinax/aws;
import ballerinax/aws.sqs;
```

### Step 2: Instantiate a new connector

Create a new `sqs:Client` by providing the region and authentication configurations.

```ballerina
configurable string accessKeyId = ?;
configurable string secretAccessKey = ?;

sqs:Client sqsClient = check new ({
   region: aws:US_EAST_1,
   auth: {
      accessKeyId,
      secretAccessKey
   }
});
```

#### Alternative authentication methods

##### Profile-based authentication

You can use AWS profile-based authentication as an alternative to static credentials.

```ballerina
sqs:Client sqsClient = check new ({
   region: aws:US_EAST_1,
   auth: {
      profileName: "myAwsProfile",
      credentialsFilePath: "/path/to/custom/credentials"
   }
});
```

##### Default credential provider chain

The standard default credential provider chain, trying each of the following in order and taking the first source that yields credentials:

1. Environment variables (`AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`, and `AWS_WEB_IDENTITY_TOKEN_FILE` if set)
2. The shared config/credentials file's active profile (`AWS_PROFILE`, or `default` if unset) — which may itself resolve via SSO
, an external process, or a chained `AssumeRole` call, depending on that profile's configuration
3. Container credentials (ECS/EKS)
4. EC2 instance profile (IMDS)

```ballerina
sqs:Client sqsClient = check new ({
   region: aws:US_EAST_1,
   auth: auth:DEFAULT_CREDENTIALS
});
```

> **Note:** Ensure your AWS credentials file follows the standard format.
>
> ```ini
> [default]
> aws_access_key_id = YOUR_ACCESS_KEY_ID
> aws_secret_access_key = YOUR_SECRET_ACCESS_KEY
>
> [myAwsProfile]
> aws_access_key_id = ANOTHER_ACCESS_KEY_ID
> aws_secret_access_key = ANOTHER_SECRET_ACCESS_KEY
> ```


### Step 3: Invoke the connector operations

Now, utilize the available connector operations.

#### Create a queue
```ballerina
string queueUrl = check sqsClient->createQueue("my-test-queue");
```

#### Send a message
```ballerina
sqs:SendMessageResponse response = check sqsClient->sendMessage(queueUrl, "Hello from Ballerina!");
```

#### Receive messages
```ballerina
sqs:Message[] messages = check sqsClient->receiveMessage(queueUrl);
```

#### Delete a message
```ballerina
check sqsClient->deleteMessage(queueUrl, receiptHandle);
```

#### Batch operations
```ballerina
// Send multiple messages at once
sqs:SendMessageBatchEntry[] entries = [
    {id: "msg1", body: "First message"},
    {id: "msg2", body: "Second message", delaySeconds: 5}
];
sqs:SendMessageBatchResponse batchResponse = check sqsClient->sendMessageBatch(queueUrl, entries);

// Delete multiple messages at once
sqs:DeleteMessageBatchEntry[] deleteEntries = [
    {id: "del1", receiptHandle: "receipt-handle-1"},
    {id: "del2", receiptHandle: "receipt-handle-2"}
];
sqs:DeleteMessageBatchResponse deleteResponse = check sqsClient->deleteMessageBatch(queueUrl, deleteEntries);
```

#### Queue management
```ballerina
// List all queues
sqs:ListQueuesResponse queues = check sqsClient->listQueues();

// Get queue attributes
sqs:GetQueueAttributesResponse attributes = check sqsClient->getQueueAttributes(queueUrl);

// Set queue attributes
sqs:QueueAttributes newAttributes = {
    visibilityTimeout: 300,
    messageRetentionPeriod: 1209600 // 14 days
};
check sqsClient->setQueueAttributes(queueUrl, newAttributes);

// Delete a queue
check sqsClient->deleteQueue(queueUrl);
```

#### Working with FIFO queues

For First-In-First-Out (FIFO) queues, you need to provide additional parameters:

```ballerina
// Create a FIFO queue
sqs:QueueAttributes fifoAttributes = {
    fifoQueue: true,
    contentBasedDeduplication: true
};
string fifoQueueUrl = check sqsClient->createQueue("my-fifo-queue.fifo", queueAttributes = fifoAttributes);

// Send message to FIFO queue
sqs:SendMessageResponse fifoResponse = check sqsClient->sendMessage(
    fifoQueueUrl,
    "FIFO message",
    messageGroupId = "group1",
    messageDeduplicationId = "unique-id-1"
);
```

### Step 4: Run the Ballerina application

Use the following command to compile and run the Ballerina program.

```bash
bal run
```

## Examples

The `ballerinax/aws.sqs` connector provides practical examples illustrating usage in various scenarios. Explore these [examples](https://github.com/ballerina-platform/module-ballerinax-aws.sqs/tree/master/examples):

1. [**Basic Queue Consumer**](https://github.com/ballerina-platform/module-ballerinax-aws.sqs/tree/master/examples/basic-queue-consumer) – Demonstrates creating a standard SQS queue, sending messages, and consuming them using a Ballerina listener.
2. [**Basic Queue Operations**](https://github.com/ballerina-platform/module-ballerinax-aws.sqs/tree/master/examples/basic-queue-operations) – Shows how to create a queue, send, receive, and delete messages, and delete the queue.
3. [**Advanced Messaging Features**](https://github.com/ballerina-platform/module-ballerinax-aws.sqs/tree/master/examples/advanced-messaging-features) – Demonstrates advanced messaging features such as message attributes, batch sending, and custom queue attributes.
4. [**FIFO Queue**](https://github.com/ballerina-platform/module-ballerinax-aws.sqs/tree/master/examples/fifo-queue) – Shows how to work with FIFO queues, including sending messages with different `messageGroupId`s and grouping received messages.

// --- END README ---

// --- Types ---

const string ALL = "All";

const string SENDER_ID = "SenderId";

const string SENT_TIMESTAMP = "SentTimestamp";

const string APPROXIMATE_RECEIVE_COUNT = "ApproximateReceiveCount";

const string APPROXIMATE_FIRST_RECEIVE_TIMESTAMP = "ApproximateFirstReceiveTimestamp";

const string SEQUENCE_NUMBER = "SequenceNumber";

const string MESSAGE_DEDUPLICATION_ID = "MessageDeduplicationId";

const string MESSAGE_GROUP_ID = "MessageGroupId";

const string AWS_TRACE_HEADER = "AWSTraceHeader";

const string DEAD_LETTER_QUEUE_SOURCE_ARN = "DeadLetterQueueSourceArn";

const string SQS_MANAGED_SSE_ENABLED = "SqsManagedSseEnabled";

const string ALLOW_ALL = "allowAll";

const string DENY_ALL = "denyAll";

const string BY_QUEUE = "byQueue";

const string MESSAGE_GROUP = "messageGroup";

const string QUEUE = "queue";

const string PER_MESSAGE_GROUP_ID = "perMessageGroupId";

const string PER_QUEUE = "perQueue";

const string POLICY = "Policy";

const string VISIBILITY_TIMEOUT = "VisibilityTimeout";

const string MAXIMUM_MESSAGE_SIZE = "MaximumMessageSize";

const string MESSAGE_RETENTION_PERIOD = "MessageRetentionPeriod";

const string APPROXIMATE_NUMBER_OF_MESSAGES = "ApproximateNumberOfMessages";

const string APPROXIMATE_NUMBER_OF_MESSAGES_NOT_VISIBLE = "ApproximateNumberOfMessagesNotVisible";

const string CREATED_TIMESTAMP = "CreatedTimestamp";

const string LAST_MODIFIED_TIMESTAMP = "LastModifiedTimestamp";

const string QUEUE_ARN = "QueueArn";

const string APPROXIMATE_NUMBER_OF_MESSAGES_DELAYED = "ApproximateNumberOfMessagesDelayed";

const string DELAY_SECONDS = "DelaySeconds";

const string RECEIVE_MESSAGE_WAIT_TIME_SECONDS = "ReceiveMessageWaitTimeSeconds";

const string REDRIVE_POLICY = "RedrivePolicy";

const string FIFO_QUEUE = "FifoQueue";

const string CONTENT_BASED_DEDUPLICATION = "ContentBasedDeduplication";

const string KMS_MASTER_KEY_ID = "KmsMasterKeyId";

const string KMS_DATA_KEY_REUSE_PERIOD_SECONDS = "KmsDataKeyReusePeriodSeconds";

const string DEDUPLICATION_SCOPE = "DeduplicationScope";

const string FIFO_THROUGHPUT_LIMIT = "FifoThroughputLimit";

const string REDRIVE_ALLOW_POLICY = "RedriveAllowPolicy";

// Unknown type: Error

# Represents the connection configuration for the Amazon SQS client.
# 

type ConnectionConfig record {
    # Authentication configuration: any standard credential source supported by
AWS — static credentials, an AWS profile, STS assume-role,
web identity (OIDC), IAM Identity Center (SSO), an external credential
process, or the default credential provider chain
    $CompilationError$ auth;
    # AWS region: an `aws:Region` enum member or a plain region
string (e.g., `"us-east-1"`) for regions not yet in the enum
    $CompilationError$|string region;
    # Optional endpoint options: FIPS/dualstack variants, or a custom
endpoint override (e.g. LocalStack, VPC interface endpoints)
    $CompilationError$ endpoint?;
};

# Contains response details returned by the `sendMessage` API.
# 

type SendMessageResponse record {
    # Unique ID assigned to the message
    string messageId;
    # MD5 digest of the non-URL-encoded message body
    string md5OfMessageBody;
    # MD5 digest of the non-URL-encoded message attribute string
    string md5OfMessageAttributes?;
    # MD5 digest of the non-URL-encoded system attribute string
    string md5OfMessageSystemAttributes?;
    # Message sequence number for FIFO queues
    string sequenceNumber?;
};

# Represents optional parameters for sending messages to Amazon SQS.
# 

type SendMessageConfig record {
    # Duration to delay the message, in seconds (0 to 900)
    int delaySeconds?;
    # Custom attributes to attach to the message
    map<ballerinax/aws.sqs:5.0.0:MessageAttributeValue> messageAttributes?;
    # X-Ray tracing header for distributed tracing support
    string awsTraceHeader?;
    # Token for deduplicating messages (FIFO only)
    string messageDeduplicationId?;
    # Tag specifying the message group (FIFO only)
    string messageGroupId?;
};

# Represents a user-defined message attribute sent with a message.
# 

type MessageAttributeValue record {
    # Type of the attribute: `String`, `Number`, or `Binary`
    string dataType;
    # Optional string value
    string stringValue?;
    # Optional binary data (e.g., encrypted or compressed)
    byte[] binaryValue?;
};

# Represents optional parameters for receiving messages from an SQS queue.
# 

type ReceiveMessageConfig record {
    # Duration to wait for a message to arrive (long polling)
    int waitTimeSeconds?;
    # Visibility timeout for received messages
    int visibilityTimeout?;
    # Maximum number of messages to receive (1 to 10)
    int maxNumberOfMessages?;
    # Deduplication token for `receiveMessage` requests (FIFO only)
    string receiveRequestAttemptId?;
    # List of message attribute names to return; use `All` to get all
    string[] messageAttributeNames?;
    # List of system attribute names to return; use `All` to get all
    MessageSystemAttributeName[] messageSystemAttributeNames?;
};

# Represents supported system attribute names for SQS messages.
enum MessageSystemAttributeName {
    SQS_MANAGED_SSE_ENABLED,
    DEAD_LETTER_QUEUE_SOURCE_ARN,
    AWS_TRACE_HEADER,
    MESSAGE_GROUP_ID,
    MESSAGE_DEDUPLICATION_ID,
    SEQUENCE_NUMBER,
    APPROXIMATE_FIRST_RECEIVE_TIMESTAMP,
    APPROXIMATE_RECEIVE_COUNT,
    SENT_TIMESTAMP,
    SENDER_ID,
    ALL
}

# Represents a message received using the `receiveMessage` API.
# 

type Message record {
    # System-defined attributes associated with the message
    MessageAttributes messageSystemAttributes?;
    # Content of the message
    string body?;
    # MD5 digest of the non-URL-encoded message body
    string md5OfBody?;
    # MD5 digest of the non-URL-encoded attribute string
    string md5OfMessageAttributes?;
    # User-defined attributes attached to the message
    map<ballerinax/aws.sqs:5.0.0:MessageAttributeValue> messageAttributes?;
    # Unique ID assigned to the message
    string messageId?;
    # Token required to delete or change visibility of the message
    string receiptHandle?;
};

# Represents the system attributes of an Amazon SQS message.
# 

type MessageAttributes record {
    # Number of times the message has been received across all queues
    int approximateReceiveCount?;
    # Time (in epoch milliseconds) when the message was first received
    int approximateFirstReceiveTimestamp?;
    # X-Ray trace header used for distributed tracing
    string awsTraceHeader?;
    # Token used to deduplicate messages within a 5-minute deduplication interval (FIFO only)
    string messageDeduplicationId?;
    # Tag specifying the message group for ordered processing (FIFO only)
    string messageGroupId?;
    # AWS account ID of the message sender
    string senderId?;
    # Time (in epoch milliseconds) when the message was sent to the queue
    int sentTimeStamp?;
    # Unique, non-consecutive number assigned to the message (FIFO only)
    string sequenceNumber?;
    # ARN of the dead-letter queue where the message was moved
    string deadLetterQueueSourceArn?;
};

# Represents a single message entry in a `sendMessageBatch` request.
# 

type SendMessageBatchEntry record {
    # Unique ID for the batch message entry (up to 80 characters)
    string id;
    # The body of the message
    string body;
    int delaySeconds?;
    map<ballerinax/aws.sqs:5.0.0:MessageAttributeValue> messageAttributes?;
    string awsTraceHeader?;
    string messageDeduplicationId?;
    string messageGroupId?;
};

# Represents the response returned by the `sendMessageBatch` operation.
# 

type SendMessageBatchResponse record {
    # List of successfully enqueued messages
    SendMessageBatchResultEntry[] successful;
    # List of failed messages with error details
    BatchResultErrorEntry[] failed;
};

# Represents a successful result entry from a `sendMessageBatch` response.
# 

type SendMessageBatchResultEntry record {
    # Batch message ID
    string id;
    # MD5 digest of the message body
    string md5OfMessageBody;
    # ID assigned to the message
    string messageId;
    # MD5 digest of the message attributes
    string md5OfMessageAttributes?;
    # MD5 digest of the system attributes
    string md5OfMessageSystemAttributes?;
    # Unique message sequence number (FIFO only)
    string sequenceNumber?;
};

# Represents an error entry in a `sendMessageBatch` or `deleteMessageBatch` response.
# 

type BatchResultErrorEntry record {
    # Batch entry ID that failed
    string id;
    # Error code describing the failure
    string code;
    # Whether the error was caused by the sender
    boolean senderFault;
    # Additional error details
    string message?;
};

# Represents a single message entry in a `deleteMessageBatch` request.
# 

type DeleteMessageBatchEntry record {
    # Batch entry ID for the receipt handle
    string id;
    # The receipt handle of the message to delete
    string receiptHandle;
};

# Represents the response returned by the `deleteMessageBatch` operation.
# 

type DeleteMessageBatchResponse record {
    # List of successfully deleted message entries
    DeleteMessageBatchResultEntry[] successful;
    # List of failed message entries with error details
    BatchResultErrorEntry[] failed;
};

# Represents a successful result entry from a `deleteMessageBatch` response.
# 

type DeleteMessageBatchResultEntry record {
    # Batch entry ID of the successfully deleted message
    string id;
};

# Represents the configuration used when creating an SQS queue.
# 

type CreateQueueConfig record {
    # Optional queue attributes to apply
    QueueAttributes queueAttributes?;
    # Optional cost allocation tags to associate with the queue
    map<string> tags?;
};

# Represents configurable attributes for an Amazon SQS queue.
# 

type QueueAttributes record {
    # Time (in seconds) to delay delivery of all messages
    int delaySeconds?;
    # Maximum allowed size (in bytes) for a message
    int maximumMessageSize?;
    # Time (in seconds) that messages are retained
    int messageRetentionPeriod?;
    # JSON-formatted access policy for the queue
    string policy?;
    # Time (in seconds) to wait for a message in long polling
    int receiveMessageWaitTimeSeconds?;
    # Visibility timeout (in seconds) for messages
    int visibilityTimeout?;
    # Dead-letter queue configuration for this queue
    RedrivePolicy redrivePolicy?;
    # Permissions for queues that can specify this one as a dead-letter queue
    RedriveAllowPolicy redriveAllowPolicy?;
    # AWS KMS key ID used for server-side encryption
    string kmsMasterKeyId?;
    # Reuse duration (in seconds) for data keys in SSE
    string kmsDataKeyReusePeriodSeconds?;
    # Enables SQS-managed server-side encryption
    boolean sqsManagedSseEnabled?;
    # Whether the queue is FIFO; must be set at creation time
    boolean fifoQueue?;
    # Enables content-based deduplication (FIFO only)
    boolean contentBasedDeduplication?;
    # Scope for message deduplication (FIFO high throughput only)
    DeduplicationScope deduplicationScope?;
    # Throughput quota mode (FIFO high throughput only)
    FifoThroughputLimit fifoThroughputLimit?;
};

# Represents a dead-letter queue redrive policy.
# 

type RedrivePolicy record {
    # ARN of the dead-letter queue
    string deadLetterTargetArn?;
    # Maximum number of receives before moving message to the DLQ
    int maxReceiveCount?;
};

# Represents permissions for queues to specify another as a dead-letter queue.
# 

type RedriveAllowPolicy record {
    # Allowed permission type
    RedrivePermission redrivePermission?;
    # List of ARNs allowed to use this queue as a DLQ (if `byQueue`)
    string[] sourceQueueArns?;
};

# Represents allowed redrive permission values.
# 
# - `allowAll` – All queues in the account can use this queue as a DLQ (default)
# - `denyAll` – No queues can use this queue as a DLQ
# - `byQueue` – Only specified source queues can use this queue as a DLQ
enum RedrivePermission {
    BY_QUEUE,
    DENY_ALL,
    ALLOW_ALL
}

# Represents deduplication scope for high throughput FIFO queues.
# 
# - `messageGroup` – Deduplication is scoped per message group
# - `queue` – Deduplication is scoped across the entire queue
enum DeduplicationScope {
    QUEUE,
    MESSAGE_GROUP
}

# Represents throughput limit mode for high throughput FIFO queues.
# 
# - `perMessageGroupId` – Throughput quota is per message group
# - `perQueue` – Throughput quota is shared across the entire queue
enum FifoThroughputLimit {
    PER_QUEUE,
    PER_MESSAGE_GROUP_ID
}

# Represents the optional configuration for the `getQueueUrl` operation.
# 

type GetQueueUrlConfig record {
    # AWS account ID of the queue owner. Required when accessing a queue owned by another AWS account
    string queueOwnerAWSAccountId?;
};

# Represents the optional parameters for the `listQueues` operation.
# 

type ListQueuesConfig record {
    # Maximum number of results to return. Required to receive a `nextToken` in the response.
    int maxResults?;
    # Token to retrieve the next set of results in paginated responses
    string nextToken?;
    # Prefix to filter queue names. Only queues that start with this value are returned
    string queueNamePrefix?;
};

# Represents the response from the `listQueues` operation.
# 

type ListQueuesResponse record {
    # List of matching queue URLs
    string[] queueUrls;
    # Pagination token for the next request. `null` if there are no more results.
    string nextToken?;
};

# Represents the optional parameters for the `getQueueAttributes` operation.
# 

type GetQueueAttributesConfig record {
    # List of queue attributes to retrieve
    QueueAttributeName[] attributeNames?;
};

# Represents the attributes that can be retrieved from a queue.
enum QueueAttributeName {
    SQS_MANAGED_SSE_ENABLED,
    REDRIVE_ALLOW_POLICY,
    FIFO_THROUGHPUT_LIMIT,
    DEDUPLICATION_SCOPE,
    KMS_DATA_KEY_REUSE_PERIOD_SECONDS,
    KMS_MASTER_KEY_ID,
    CONTENT_BASED_DEDUPLICATION,
    FIFO_QUEUE,
    REDRIVE_POLICY,
    RECEIVE_MESSAGE_WAIT_TIME_SECONDS,
    DELAY_SECONDS,
    APPROXIMATE_NUMBER_OF_MESSAGES_DELAYED,
    QUEUE_ARN,
    LAST_MODIFIED_TIMESTAMP,
    CREATED_TIMESTAMP,
    APPROXIMATE_NUMBER_OF_MESSAGES_NOT_VISIBLE,
    APPROXIMATE_NUMBER_OF_MESSAGES,
    MESSAGE_RETENTION_PERIOD,
    MAXIMUM_MESSAGE_SIZE,
    VISIBILITY_TIMEOUT,
    POLICY,
    ALL
}

# Represents the response from the `getQueueAttributes` operation.
# 

type GetQueueAttributesResponse record {
    # Map of queue attribute names to their values
    map<string> queueAttributes;
};

# Represents the response from the `listQueueTags` operation.
# 

type ListQueueTagsResponse record {
    # Map of tags added to the specified queue
    map<string> tags;
};

# Represents the configuration for starting a message movement task.
# 

type StartMessageMoveTaskConfig record {
    # ARN of the destination queue. If not set, messages are redriven to their original source queues
    string destinationARN?;
    # Fixed message movement rate. Max value is 500. If not set, the system optimizes the rate.
    int maxNumberOfMessagesPerSecond?;
};

# Represents the response from the `startMessageMoveTask` operation.
# 

type StartMessageMoveTaskResponse record {
    # Unique identifier for the initiated message movement task. Use to cancel the task if needed
    string taskHandle;
};

# Represents the response from the `cancelMessageMoveTask` operation.
# 

type CancelMessageMoveTaskResponse record {
    # Number of messages moved before the task was cancelled
    int approximateNumberOfMessagesMoved;
};

# Polling configuration for message retrieval.
# 

type PollingConfig record {
    # Interval between polling attempts in seconds. If set to 0, the listener will poll back-to-back without delay. Use with caution as it may cause high CPU usage.
    decimal pollInterval?;
    # The duration, in seconds, for which the polling waits for messages
    int waitTime?;
    # The duration, in seconds, for which the received message remains invisible to other consumers
    int visibilityTimeout?;
};

# Represents an AWS SQS service object that can be attached to an `sqs:Listener`.
class Service {
}

# The service configuration type for the `sqs:Service`.

type ServiceConfigType record {
    # The URL of the SQS queue to consume messages from
    string queueUrl;
    # Optional per-service polling behavior
    PollingConfig config?;
    # Whether to automatically delete messages after receiving
    boolean autoDelete?;
};

// Unknown type: Listener

// --- Client ---

# Provides control over message deletion from the queue after processing.
client class Caller {
    function init() returns sqs:sqs:Caller;

    # Delete the message from the queue after successful processing.
    # 
    remote function delete() returns Error|();
}

# The Amazon SQS API Client.
# 
# Provides access to Amazon Simple Queue Service (SQS) using the AWS SDK for Java V2.
# Supports static credentials, profile-based credentials, and the default AWS credential
# provider chain (environment variables, ECS container credentials, EC2 instance profiles, etc.).
client class Client {
    function init(Unknown Type auth = , Unknown Type|string region = , Unknown Type endpoint = , ConnectionConfig connectionConfig) returns ballerinax/aws.sqs:5.0.0:Error?;

    # Delivers a message to the specified SQS queue.
    # 
    remote function sendMessage(string queueUrl, string messageBody, int delaySeconds = 0, map<sqs:MessageAttributeValue> messageAttributes = {}, string awsTraceHeader = "", string messageDeduplicationId = "", string messageGroupId = "", SendMessageConfig sendMessageConfig) returns SendMessageResponse|Error;

    # Retrieves one or more messages from the specified queue.
    # 
    remote function receiveMessage(string queueUrl, int waitTimeSeconds = 0, int visibilityTimeout = 0, int maxNumberOfMessages = 0, string receiveRequestAttemptId = "", string[] messageAttributeNames = [], MessageSystemAttributeName[] messageSystemAttributeNames = [], ReceiveMessageConfig receiveMessageConfig) returns Message[]|Error;

    # Deletes a specified message from an Amazon SQS queue using the given receipt handle.
    # 
    remote function deleteMessage(string queueUrl, string receiptHandle) returns Error|();

    # Sends up to 10 messages as a batch to the specified Amazon SQS queue.
    # 
    remote function sendMessageBatch(string queueUrl, SendMessageBatchEntry[] entries) returns SendMessageBatchResponse|Error;

    # Deletes up to ten messages from the specified queue. This is a batch version of `sqs:deleteMessage`.
    # The result of the action on each message is reported individually in the response.
    # 
    remote function deleteMessageBatch(string queueUrl, DeleteMessageBatchEntry[] entries) returns DeleteMessageBatchResponse|Error;

    # Creates a new Amazon SQS queue with the specified attributes and tags.
    # 
    remote function createQueue(string queueName, QueueAttributes queueAttributes = {}, map<string> tags = {}, CreateQueueConfig createQueueConfig) returns string|Error;

    # Deletes the specified Amazon SQS queue.
    # 
    remote function deleteQueue(string queueUrl) returns Error|();

    # Retrieves the URL of the specified Amazon SQS queue.
    # 
    remote function getQueueUrl(string queueName, string queueOwnerAWSAccountId = "", GetQueueUrlConfig getQueueUrlConfig) returns string|Error;

    # Lists the Amazon SQS queues in the current region. Supports filtering by name prefix and paginated results.
    # 
    remote function listQueues(int maxResults = 0, string nextToken = "", string queueNamePrefix = "", ListQueuesConfig listQueuesConfig) returns ListQueuesResponse|Error;

    # Retrieves the attributes of the specified Amazon SQS queue.
    # 
    remote function getQueueAttributes(string queueUrl, QueueAttributeName[] attributeNames = [], GetQueueAttributesConfig getQueueAttributesConfig) returns GetQueueAttributesResponse|Error;

    # Sets one or more attributes for the specified Amazon SQS queue.
    # 
    remote function setQueueAttributes(string queueUrl, QueueAttributes queueAttributes) returns Error|();

    # Changes the visibility timeout of a specific message in a queue.
    # 
    remote function changeMessageVisibility(string queueUrl, string receiptHandle, int visibilityTimeout) returns Error|();

    # Purges the specified queue, deleting all messages in it. This action is irreversible.
    # 
    remote function purgeQueue(string queueUrl) returns Error|();

    # Adds cost allocation tags to the specified Amazon SQS queue.
    # 
    # - A maximum of 50 tags per queue is recommended
    # - Tags are case-sensitive and treated as plain character strings
    # - New tags with duplicate keys overwrite existing ones
    # 
    remote function tagQueue(string queueUrl, map<string> tags) returns Error|();

    # Removes cost allocation tags from the specified Amazon SQS queue.
    # 
    remote function untagQueue(string queueUrl, string[] tags) returns Error|();

    # Lists all cost allocation tags added to the specified Amazon SQS queue.
    # 
    remote function listQueueTags(string queueUrl) returns ListQueueTagsResponse|Error;

    # Starts a message movement task to transfer messages from a dead-letter queue (DLQ) to another queue.
    # 
    # - Only supported for DLQs whose sources are other Amazon SQS queues
    # - Not supported for non-SQS sources (e.g., AWS Lambda, Amazon SNS)
    # - Only one active task is allowed per queue at any time
    # 
    remote function startMessageMoveTask(string sourceARN, string destinationARN = "", int maxNumberOfMessagesPerSecond = 0, StartMessageMoveTaskConfig startMessageMoveTaskConfig) returns StartMessageMoveTaskResponse|Error;

    # Cancels an active message movement task for the given task handle.
    # 
    # - Only applicable when the task status is `RUNNING`
    # - Already moved messages will not be reverted
    # - Only one active task is allowed per queue at any time
    # 
    remote function cancelMessageMoveTask(string taskHandle) returns CancelMessageMoveTaskResponse|Error;

    # Gracefully closes the AWS SQS client and releases all associated resources.
    # 
    remote function close() returns Error|();
}
