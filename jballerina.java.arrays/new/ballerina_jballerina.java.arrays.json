{
  "name": "ballerina/jballerina.java.arrays",
  "description": "This module provides APIs to create new Java array instances, get elements from arrays, set elements, etc. ",
  "readme": "## Overview\n\nThis module provides APIs to create new Java array instances, get elements from arrays, set elements, etc. \n\n#### Create a Java array instance\n```ballerina\nhandle stringClass \u003d check java:getClass(\"java.lang.String\");\nhandle StrArray \u003d arrays:newInstance(stringClass, 4);\n```\n\n#### Get an element from a Java array\n```ballerina\nhandle firstWord \u003d arrays:get(words, 0);\n```\n\n#### Replace an element in a Java array\n```ballerina\narrays:set(strArray, 0, java:fromString(\"Ballerina\"));\n```\n\n#### Get Java array length\n```ballerina\nint length \u003d arrays:getLength(array);\n```\n\n#### Get a Ballerina array from a Java array\n```ballerina\nint[] array \u003d \u003cint[]\u003e check arrays:fromHandle(arrayHandle, \"int\");\n```\n\n#### Get a Java array from a Ballerina array\n```ballerina\nhandle handleValue \u003d check arrays:toHandle(bArray, \"char\");\n```\n\n",
  "typeDefs": [],
  "clients": [],
  "functions": [
    {
      "name": "newInstance",
      "type": "Normal Function",
      "description": "Returns a `handle`, which refers to the new Java array instance with the specified element type and dimensions. This function completes abruptly\nwith a `panic` if the specified handle refers to a Java null or if zero dimensions have been provided.\n```ballerina\nhandle stringClass \u003d check java:getClass(\"java.lang.String\");\nhandle StrArray \u003d arrays:newInstance(stringClass, 4);\n```\n",
      "parameters": [
        {
          "name": "classType",
          "description": "The handle, which refers to the element type of the array",
          "type": {
            "name": "handle"
          }
        },
        {
          "name": "dimensions",
          "description": "The dimensions of the array",
          "type": {
            "name": "int"
          },
          "optional": true
        }
      ],
      "return": {
        "description": "The handle, which refers to the new Java array instance",
        "type": {
          "name": "handle"
        }
      }
    },
    {
      "name": "get",
      "type": "Normal Function",
      "description": "Returns a `handle`, which refers to the element at the specified index in the given Java array. This function\ncompletes abruptly with a `panic` if the specified handle refers to a Java null or if the handle does not refer\nto a Java array.\n```ballerina\nhandle words \u003d getSortedArray();\nhandle firstWord \u003d arrays:get(words, 0);\n```\n",
      "parameters": [
        {
          "name": "array",
          "description": "The `handle`, which refers to the Java array",
          "type": {
            "name": "handle"
          }
        },
        {
          "name": "index",
          "description": "The index of the element to be returned",
          "type": {
            "name": "int"
          }
        }
      ],
      "return": {
        "description": "The `handle`, which refers to the element at the specified position in the Java array",
        "type": {
          "name": "handle"
        }
      }
    },
    {
      "name": "set",
      "type": "Normal Function",
      "description": "Replaces the indexed element at the specified index in the given Java array with the specified element. This\nfunction completes abruptly with a `panic` if the specified handle refers to a Java null or if the handle does\nnot refer to a Java array.\n```ballerina\nhandle strArray \u003d getStringArray();\narrays:set(strArray, 0, java:fromString(\"Ballerina\"));\n```\n",
      "parameters": [
        {
          "name": "array",
          "description": "The `handle`, which refers to the Java array",
          "type": {
            "name": "handle"
          }
        },
        {
          "name": "index",
          "description": "The index of the element to be replaced",
          "type": {
            "name": "int"
          }
        },
        {
          "name": "element",
          "description": "The `handle`, which refers to the java element to be stored at the specified index",
          "type": {
            "name": "handle"
          }
        }
      ],
      "return": {
        "type": {
          "name": "()"
        }
      }
    },
    {
      "name": "getLength",
      "type": "Normal Function",
      "description": "Returns the length of the given Java array.\n```ballerina\nhandle array \u003d getArray();\nint length \u003d arrays:getLength(array);\n```\n",
      "parameters": [
        {
          "name": "array",
          "description": "The `handle`, which refers to the Java array",
          "type": {
            "name": "handle"
          }
        }
      ],
      "return": {
        "description": "The length of the given Java array",
        "type": {
          "name": "int"
        }
      }
    },
    {
      "name": "fromHandle",
      "type": "Normal Function",
      "description": "Returns a Ballerina array for a handle that holds a Java array.\n```ballerina\nint[] array \u003d \u003cint[]\u003e check arrays:fromHandle(arrayHandle, \"int\");\n```\n",
      "parameters": [
        {
          "name": "array",
          "description": "The `handle`, which refers to the Java array",
          "type": {
            "name": "handle"
          }
        },
        {
          "name": "jType",
          "description": "The `string` parameter provided to specify the Java array element type",
          "type": {
            "name": "string"
          }
        },
        {
          "name": "bType",
          "description": "The optional `string` parameter provided to specify the Ballerina array element type",
          "type": {
            "name": "string"
          },
          "optional": true,
          "default": "\"default\""
        }
      ],
      "return": {
        "description": "Ballerina array `any[]|error` for the provided handle",
        "type": {
          "name": "any[]|error"
        }
      }
    },
    {
      "name": "toHandle",
      "type": "Normal Function",
      "description": "Returns a handle value, which refers to the java array representation for a Ballerina array.\n```ballerina\nhandle handleValue \u003d check arrays:toHandle(array, \"char\");\n```\n",
      "parameters": [
        {
          "name": "array",
          "description": "Ballerina array which is to be converted to a handle reference",
          "type": {
            "name": "any[]"
          }
        },
        {
          "name": "jType",
          "description": "Java class name or the primitive type of the array elements referenced by the handle",
          "type": {
            "name": "string"
          }
        }
      ],
      "return": {
        "description": "The `handle` which refers to the Java array representation of the Ballerina array, or an error if the conversion fails",
        "type": {
          "name": "handle|error"
        }
      }
    }
  ],
  "services": [],
  "annotations": []
}